{
  "name": "xcx-http-fetch",
  "version": "0.0.0",
  "private": "true",
  "scripts": {
    "register": "npx xcratch-register --link -C --id=httpFetch --gui=\"../scratch-gui\"",
    "build": "npx xcratch-build --module=httpFetch --gui=\"../scratch-gui\""
  },
  "devDependencies": {
    "eslint-config-scratch": "^7.0.0"
  }
}